# Cloudflare Workers static-asset redirects.
# Copied to the root of the asset directory by `script/prepare-cf-assets.js`.
#
# The documentation is built under `/docs/`, so the asset root itself holds no
# document. Send it to the documentation entry point instead of a 404 - this
# makes preview URLs (`https://<branch>-hyperformula-docs.<subdomain>.workers.dev`)
# usable without manually appending the path.
/ /docs/ 302
