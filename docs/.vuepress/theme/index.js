// Local theme that extends `@vuepress/theme-default`. It exists solely to
// override `layouts/Layout.vue` and inject the "View as Markdown" link at the
// top of every guide page's content (see `./layouts/Layout.vue`).
module.exports = {
  extend: '@vuepress/theme-default',
};
