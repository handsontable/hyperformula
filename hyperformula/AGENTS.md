# `hyperformula/` — the calculation engine

The published `hyperformula` package: the parser, the interpreter, the dependency graph, and every built-in function. TypeScript, headless, no DOM.

- Architecture and the invariants that hold everywhere in `src/`: [`dev-docs/ARCHITECTURE.md`](../dev-docs/ARCHITECTURE.md)
- Build, bundle, and release commands: [`dev-docs/BUILD.md`](../dev-docs/BUILD.md)
- Tests, and the branch-matched private suite: [`dev-docs/TESTING.md`](../dev-docs/TESTING.md)
- Everything else: [`dev-docs/README.md`](../dev-docs/README.md)

Run package scripts from this directory, or from the repository root with `--workspace=hyperformula`.

Skill: `hyperformula-dev`.
